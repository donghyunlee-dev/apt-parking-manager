server:
  port: 9000

spring:
  application:
    name: apartment.parking.manager
  config:
    activate:
      on-profile: dev
  jackson:
    property-naming-strategy: SNAKE_CASE
  datasource:
    url: jdbc:p6spy:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&?prepareThreshold=0&preferQueryMode=simple
    username: postgres.gipaatdvbnmahvpwphtk
    password: "!@!L1e2e34!!!"
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 3000
      idle-timeout: 30000
      max-lifetime: 240000
      data-source-properties:
        cachePrepStmts: false
        prepStmtCacheSize: 0
        prepStmtCacheSqlLimit: 0

  jpa:
    properties:
      hibernate:
        type:
          bind: true
        #format_sql: true
        #use_sql_comments: false
        #show_sql: true
    open-in-view: false

logging:
  level:
    root: info
    org:
      springframework:
        web: debug
    com:
      windsoft:
        apartment_parking_manager: debug


